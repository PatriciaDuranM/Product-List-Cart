const filterElement = document.getElementById('filter-tab');

const activateFilter = () =>{
    const filter = event.target.dataset.filter{
        if(!filter) return;

        
    }


}



filterElement.addEventListener('click', activateFilter);